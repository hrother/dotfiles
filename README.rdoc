=== Holger Rother's dot files

Config files to set up my system

== Installation

  git clone git://github.com/hrother/dotfiles ~/.dotfiles

  Vim plugins are managed by vundle. Install it!
  git clone https://github.com/gmarik/vundle.git ~/.vim/bundle/vundle
